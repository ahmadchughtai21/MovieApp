import { useEffect } from 'react'

/**
 * Keyboard navigation removed per user request.
 * Hook is kept as a no-op to avoid breaking imports.
 */
export function useTVNavigation() {
  useEffect(() => {}, [])
}
